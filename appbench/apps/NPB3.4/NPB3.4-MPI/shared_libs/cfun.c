#include <stdio.h>

int reportrank_(int *rank)
{
	if(*rank == 0)
		printf("rank =%d\n", *rank);
	return 0;
}

int getrec_pages_(int *rank)
{
	if(*rank == 0)
	{
		printf("rank=%d\n", *rank);
	}
	return 0;
}
